{
  "name": "lab-jeff",
  "version": "1.0.0",
  "description": "This application supports a student database. It performs CRUD operations on the database.",
  "main": "index.js",
  "directories": {
    "test": "test"
  },

  "scripts": {
    "start-db": "mkdir -p ./db && mongod --dbpath ./db",
    "stop-db": "killall mongod",
    "start": "node index.js",
    "test": "mocha"
  },

  "dependencies": {
    "body-parser": "^1.17.2",
    "cors": "^2.8.3",
    "dotenv": "^4.0.0",
    "expect": "^1.20.2",
    "express": "^4.15.3",
    "http-errors": "^1.6.1",
    "mongoose": "^4.11.0",
    "morgan": "^1.8.2",
    "superagent": "^3.5.2"
  },

  "devDependencies": {
    "mocha": "^3.4.2"
  },

  "keywords": [],
  "author": "",
  "license": "ISC"
}
